# Agent Mail Repository Instructions

## Language

- Most Important: Always respond in Simplified Chinese unless the user explicitly asks for another language.

## Repository workflow

- Use `gh` for GitHub repository operations in this project.
- The active GitHub account for this repository is `xrdavies`.
- Prefer HTTPS remotes managed by `gh`, not SSH remotes.
- When creating or managing remotes, branches, or pull requests, use `gh` first.

## Git workflow

- Git commit messages must follow Conventional Commits.
- After completing a coherent modification set, create a focused commit.
- After verification, push commits to the remote repository.
